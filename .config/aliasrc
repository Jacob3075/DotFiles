alias grep='grep --color'
alias sgrep='grep -R -n -H -C 5 --exclude-dir={.git,node_modules,CVS,build,target} '

alias dud='du -d 1 -h'
alias duf='du -sh *'
alias ff='find . -type f -name'

alias h='history'
alias hgrep="fc -El 0 | grep"
alias help='man'
alias p='ps -f'
alias sortnr='sort -n -r'
alias unexport='unset'


###### My Aliases ######
alias cnc='cd && clear'
alias please='sudo'
alias free='free -h'
alias du='du -h'
alias ports='sudo lsof -i -P -n | grep LISTEN'

alias ...='cd ../..'
alias ....='cd ../../..'
alias .....='cd ../../../..'

alias ls='exa -F --icons --group-directories-first'
alias lr='exa -RF --icons --group-directories-first'
alias ll='exa -lF --icons --group-directories-first'
alias lt='exa -TF --icons --group-directories-first'
alias ld='exa -DF --icons '
alias la='exa -aF --icons --group-directories-first'
alias ldt='exa -DFT --icons '
alias llt='exa -lFT --icons --group-directories-first'
alias lla='exa -alF --icons --group-directories-first'
alias llat='exa -alFT --icons --group-directories-first'

# alias update='aptf update && aptf upgrade'
# alias clean='please apt autoremove && please apt autoclean'
# alias aptf='apt-fast'

alias pdf='evince'

alias c='clear'
alias l='ls'
alias b='bat'
alias v='nvim'

alias vd='GIT_DIR=$HOME/dotfiles/ GIT_WORK_TREE=$HOME nvim'
alias nd='GIT_DIR=$HOME/dotfiles/ GIT_WORK_TREE=$HOME nvim'

alias protonvpn='please protonvpn'
alias pvpnc='protonvpn c'
alias pvpnd='protonvpn d'

alias config="/usr/bin/git --git-dir=$HOME/dotfiles --work-tree=$HOME"

alias cp="cp -i"
alias mv='mv -i'
alias rm='rm -i'

alias yay='paru'

# Cleanup orphaned packages
# alias cleanup='sudo pacman -Rns $(pacman -Qtdq)'

# Aliases for software managment pacman or pm
alias pacman='sudo pacman --color auto'
alias update='sudo pacman -Syu'

# yay as aur helper - updates everything
alias upall="yay -Syu"

# add new fonts
alias update-fc='sudo fc-cache -fv'

# copy/paste all content of /etc/skel over to home folder - backup of config created - beware
# alias skel='cp -Rf ~/.config ~/.config-backup-$(date +%Y.%m.%d-%H.%M.%S) && cp -rf /etc/skel/* ~'

